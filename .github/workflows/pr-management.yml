name: PR Management

on:
    schedule:
        - cron: "0 0 * * *"
    workflow_dispatch:

permissions:
    contents: write
    pull-requests: write

jobs:
    manage:
        uses: wow-look-at-my/actions/.github/workflows/pr-management.yml@master
